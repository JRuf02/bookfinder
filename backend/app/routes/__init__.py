"""Logic for the API routes.

One file for each API route (e.g. "/api/books" -> routes/books.py).
This module will be used by server.py to handle API requests.
The functions in this module call the db package and other helper functions
if more complex logic is needed.
"""
